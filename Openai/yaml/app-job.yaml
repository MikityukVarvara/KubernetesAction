apiVersion: batch/v1
kind: Job
metadata:
  name: my-job
spec:
  template:
    metadata:
      name: my-job
    spec:
      containers:
      - name: my-job-container
        image: busybox
        command: ["echo", "Hello from the job container"]
      restartPolicy: Never
